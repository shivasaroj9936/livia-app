import { EmptyFieldPipePipe } from './empty-field-pipe.pipe';

describe('EmptyFieldPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyFieldPipePipe();
    expect(pipe).toBeTruthy();
  });
});
